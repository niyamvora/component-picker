{
  "name": "component-picker",
  "version": "1.4.1",
  "private": true,
  "description": "Hover any component, click, and copy its HTML + resolved CSS + interaction states + responsive diffs as an AI-ready bundle.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run build && ./test/check.sh && node test/e2e.mjs && node test/zip.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
